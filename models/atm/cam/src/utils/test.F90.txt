! program name
!     use fftw_mkl
! implicit none
! integer, save :: ifax(13)  
! real(8), allocatable, save :: trigs(:)        ! reentrant code??
! real(8), allocatable, save :: WORK(:)        
! real(8), allocatable, save :: A(:)        

! integer inc,lot,N,jump,M,i,k
! inc=1 
! lot=2
! N=8
! M=lot
! allocate( trigs(3*N/2+1) )
! allocate( WORK(M*(N+1)) )
! allocate(A(M*(N+2)))
! jump=N+2
! !init both fft
! call SC_SETUP(jump,N,lot)
! call SET99(TRIGS,IFAX,N)
! do i=1,M*(N+2)
!     A(i)=2
!  enddo
! call SC_FFT991(A,jump,N,lot,1)
! !call FFT99 (A,WORK,TRIGS,IFAX,1,JUMP,N,M,1)
! open (3,file='./out.bin',form="unformatted",access="sequential")
! do k = 1, M*(N+2)
!     write(3) A(k)
!   end do 
!   close(3)
! do i=1,M*(N+2)
!     print *,i,A(i)
!  enddo

! !  open(FID,file='binaryfile')
! !  do k = 1, N
! !    write(FID) x(k)
! !  end do 
! !  close(FID)

!  do i=1,M*(N+2)
!     A(i)=2
!  enddo
! !call SC_FFT991(A,jump,N,lot,1)

! call FFT991 (A,WORK,TRIGS,IFAX,1,JUMP,N,M,1)
! open (3,file='./out.bin1',form="unformatted",access="sequential")
! do k = 1, M*(N+2)
!     write(3) A(k)
!   end do 
!   close(3)
! do i=1,M*(N+2)
!     print *,i,A(i)
!  enddo
! end program name